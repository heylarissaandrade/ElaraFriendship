import React from "react";
import { Text, View } from "react-native";

export function WalkFiltersChips(props: any) {
  return (
    <View {...props}>
      <Text>WalkFiltersChips (stub)</Text>
    </View>
  );
}

export type WalkFilterTag = string;

export default WalkFiltersChips;
