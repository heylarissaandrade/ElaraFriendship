import React from "react";
import { Text, View } from "react-native";

export function SafetyBanner(props: any) {
  return (
    <View {...props}>
      <Text>SafetyBanner (stub)</Text>
    </View>
  );
}

export function ActiveTrackingBanner(props: any) {
  return (
    <View {...props}>
      <Text>ActiveTrackingBanner (stub)</Text>
    </View>
  );
}

export default SafetyBanner;
