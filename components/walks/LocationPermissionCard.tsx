import React from "react";
import { Text, View } from "react-native";

export function LocationPermissionCard(props: any) {
  return (
    <View {...props}>
      <Text>LocationPermissionCard (stub)</Text>
    </View>
  );
}

export default LocationPermissionCard;
