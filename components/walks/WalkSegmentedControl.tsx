import React from "react";
import { Text, View } from "react-native";

export function WalkSegmentedControl(props: any) {
  return (
    <View {...props}>
      <Text>WalkSegmentedControl (stub)</Text>
    </View>
  );
}

export default WalkSegmentedControl;
