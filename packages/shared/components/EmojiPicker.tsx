import React from "react";
import { Text, View } from "react-native";

export function EmojiPicker(props: any) {
  return (
    <View {...props}>
      <Text>EmojiPicker (stub)</Text>
    </View>
  );
}

export default EmojiPicker;
