import React from 'react'
import { ScrollView, ScrollViewProps } from 'react-native'

const ParallaxScrollView: React.FC<ScrollViewProps> = (props) => {
  return <ScrollView {...props} />
}

export default ParallaxScrollView
