import React from 'react';
import { View, ViewStyle } from 'react-native';

type Props = { name?: string; size?: number; color?: string; style?: ViewStyle }

export const IconSymbol: React.FC<Props> = () => {
  return <View />
}

export default IconSymbol
