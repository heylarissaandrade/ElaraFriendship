import React from "react";
import { Text, View } from "react-native";

export function WalkCard(props: any) {
  return (
    <View {...props}>
      <Text>WalkCard (stub)</Text>
    </View>
  );
}

export default WalkCard;
